#!/usr/bin/env bash

SOUNDS_ROOT='/usr/share/sounds/freedesktop/stereo'
sound="$1"
volume="${2:-30}"
if [[ "$sound" == "list" ]]; then
	ls -l "$SOUNDS_ROOT"
else
	SOUND="${SOUNDS_ROOT}/${sound}.oga"
	mpv --volume "$volume" "$SOUND" >/dev/null 2>&1
fi
