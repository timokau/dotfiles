#! /bin/sh
# sums up the total CPU load of all running programs
cores="$(grep -i processor /proc/cpuinfo | wc -l)"
ps -eo pcpu | awk '
BEGIN {
	sum=0.0f
} 
{
	sum +=$1
}
END {
	totalper = sum / '"$cores"'
	printf ("%3d%%\n", int(totalper+0.5))
}'
