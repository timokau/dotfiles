#!/usr/bin/env nix-shell
#! nix-shell -i bash -p xautolock xorg.xset

if [[ "$1" == "enable" ]]; then
	xset +dpms
	xautolock -enable
	echo "Autlock enabled"
else
	xset -dpms
	xautolock -disable || pkill xautolock
	echo "Autlock disabled"
fi
